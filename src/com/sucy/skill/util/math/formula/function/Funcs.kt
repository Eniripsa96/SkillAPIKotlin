package com.sucy.skill.util.math.formula.function

/**
 * SkillAPIKotlin © 2018
 */
enum class Funcs(val func: Func) {
    ABS(Abs),
    CEIL(Ceil),
    COS(Cos),
    FLOOR(Floor),
    LOG(Log),
    SIGN(Sign),
    SIN(Sin),
    SQ(Sq),
    SQRT(Sqrt),
    TAN(Tan)
}